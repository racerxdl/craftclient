#pragma once

#include <math/vectors.h>

namespace ProtoRock {
namespace Minecraft {
    using BlockPos = CraftBlock::VecI3;
    using ChunkPos = CraftBlock::VecI2;
    using SubChunkPos = CraftBlock::VecI3;
}
}